模型文件不是必须的，若选择了相关参数，节点运行时会自动下载。
如果无法下载，将 elementEasy 文件夹，剪切或拷贝到 ComfyUI\models 根目录下。

Model files are not required; if the relevant parameters are selected, they will be automatically downloaded at runtime. 

If downloading fails, cut or copy the elementEasy folder to the ComfyUI\models root directory.